# CEP Sync Service — variables d'environnement (exemple)
# Clés publiques d'appareils provisionnés (générées par `crypto-cli keygen`).
# ⚠️ Valeurs de DÉMO — en production, alimentation via PKI / Django.

SYNC_ADDR=:8090
# Forme : device_id:public_hex (séparés par virgule)
DEVICE_PUBLIC_KEYS=DEV-001:50f6f9b300badea15704311809b32c04874dfa18c487e9c961cd5197d93d73ec
